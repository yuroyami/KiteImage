POM_NAME=KiteImage
POM_DESCRIPTION=Pure-Kotlin image codec core (BMP, PNG, ... — format sniffing, ARGB pixel buffer, malformed-input guards) — a from-scratch KMP port of the canonical references. kotlin-stdlib only; Android, iOS, JVM, JS, WASM.
